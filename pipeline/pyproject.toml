[project]
name = "niklo-pipeline"
version = "0.1.0"
description = "scrape -> dedupe -> dim_venue, then push to d1"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb]>=1.5",
    "dbt-duckdb>=1.9",
    "duckdb>=1.1",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
