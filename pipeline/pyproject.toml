[project]
name = "niklo-pipeline"
version = "0.1.0"
description = "scrape -> dedupe -> dim_venue, then export a static catalog"
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.9",
    "duckdb>=1.1",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
